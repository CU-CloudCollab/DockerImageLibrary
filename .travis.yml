sudo: required

services:
  - docker

matrix:
  include:
    - env:
      - SUBFOLDER=cwmonitor
      - REPO=cucloudcollab/cwmonitor
      - TAG=$TRAVIS_BUILD_NUMBER
      services:
        - docker

script:
  - docker build $SUBFOLDER -t $REPO:$TAG

after_success:
  - docker images
